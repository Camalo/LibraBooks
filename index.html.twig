{% extends 'base.html.twig' %}

{% block title %}Hello BookController!{% endblock %}

{% block body %}

   <div class="container">
        <h2>List of books</h2>
        <div class="row">
        <div class="col-3">
            {% for category in categoryes %}
                <div>{{ category.title }}</div>
                <div class="w-100">
                </div>
            {% endfor %}
        </div>

        <div class="col-9">
        {% for book in books %}
        <div style="box-shadow: 2px 3px 20px 4px rgba(20,10,30,0.1); background-color: #F0FFF0">

            <div class="row" style="height: 150px; padding: 20px ">
                <div class="col-2 text-center">
                    <img src="/images/cat.jpg" alt="book picture" width="120" height="120" />
                </div>
                <div class="col-7">
                    <p style="font-size: 20px">{{ book.title }} </p>
                    <div>By {{ book.author }} </div>
                    <div>{{ book.category }} </div>
                    <div><a href="{{ path('one_book',{'book': book.id}) }}">подробнее</a></div>
                </div>
            </div>
        </div>

        {% endfor %}
        </div>
        </div>


{% endblock %}
